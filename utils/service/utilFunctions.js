export const utilFunctions = {
    addTwoNumber: (a, b) => a + b,
    subtractTwoNumber: (a, b) => a - b,
    multiplyTwoNumber: (a, b) => a * b,
    divideTwoNumber: (a, b) => a / b,
}

export function add(a, b) { 
    return a + b;
}
